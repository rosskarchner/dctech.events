<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Locations - DC Tech Events</title>
  <meta name="description" content="Browse tech events by location in the Washington DC area">
  <link rel="stylesheet" href="/static/css/main.css">
  <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body>
  {{> header}}

  <main class="container">
    <div class="page-header">
      <h1>Events by Location</h1>
    </div>

    <section class="locations-list">
      {{#if locations}}
        <div class="location-grid">
          {{#each locations}}
            <a href="/locations/{{this.state}}/" class="location-card">
              <h3>{{this.location}}</h3>
              <span class="event-count">{{this.count}} events</span>
            </a>
          {{/each}}
        </div>
      {{else}}
        <p>No locations found.</p>
      {{/if}}
    </section>
  </main>

  {{> footer}}
</body>
</html>
