Always suggest a simpler way, if there is one.

Do not offer praise.

Favor using official AWS javascript libraries where it could reduce the amount of code we write.

Rememeber that all API submissions (unless I instruct otherwise) should expect form-encoded input and return HTML output, as we use HTMX.