# Core web framework
flask>=2.3.0
Frozen-Flask
flask-htmx>=0.2.0

# Date/time handling
pytz
dateparser

# Data formats
pyyaml
icalendar
requests

# Calendar aggregation (used by refresh_calendars.py)
extruct
w3lib
recurring-ical-events

# AWS services (optional - for address normalization)
boto3

# Image generation
Pillow>=10.0.0

# Dev/testing only (not needed for CI build, but kept for local development)
beautifulsoup4
responses
